.message
  .info
    %p.info__user-name
      = message.user.name
    %p.info__date
      = message.created_at.strftime("%Y/%m/%d %H:%M")
  .body
    - if message.body.present?
      %p.body
        = message.body 
    = image_tag message.image.url, class: 'body__image' if message.image.present?
